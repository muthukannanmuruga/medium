// In a file named recoilState.ts or any appropriate name
import { atom } from 'recoil';

export const isLoadingState = atom({
  key: 'isLoadingState',
  default: false,
});


export const signupisLoadingState = atom({
    key: 'signupisLoadingState',
    default: false,
  });
  